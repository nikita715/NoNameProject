<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
      http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
      http://www.springframework.org/schema/context
      http://www.springframework.org/schema/context/spring-context-3.1.xsd
      http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <bean id="springDataSource" class="oracle.jdbc.pool.OracleDataSource">
        <property name="driverType">
            <value>${spring.datasource.driver-class-name}</value>
        </property>
        <property name="URL">
            <value>${spring.datasource.url}</value>
        </property>
        <property name="user">
            <value>${spring.datasource.username}</value>
        </property>
        <property name="password">
            <value>${spring.datasource.password}</value>
        </property>
    </bean>

    <!--<security:authentication-manager>-->
        <!--<security:authentication-provider>-->
            <!--<security:jdbc-user-service data-source-ref="springDataSource" users-by-username-query="SELECT NAME, PASSWORD FROM USERS WHERE NAME = '?'"/>-->
        <!--</security:authentication-provider>-->
    <!--</security:authentication-manager>-->

    <context:property-placeholder location="application.properties" />
</beans>